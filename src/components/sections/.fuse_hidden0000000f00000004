"use client";
import { experience } from "@/data/portfolio";

export function Experience() {
  return (
    <section id="experience" style={{ padding: "80px 24px" }}>
      <div style={{ maxWidth: 860, margin: "0 auto" }}>
        <div style={{ color: "#1a2540", fontSize: 13, marginBottom: 8 }}>// Work history</div>
        <h2 style={{ color: "#4ade80", fontSize: 22, fontWeight: 700, marginBottom: 40 }}>
          $ cat experience.log
        </h2>

        <div style={{ position: "relative" }}>
          {/* Timeline line */}
          <div style={{
            position: "absolute", left: 0, top: 8, bottom: 8,
            width: 1, backgroundColor: "#1a2540",
          }} />

          <div style={{ display: "flex", flexDirection: "column", gap: 32, paddingLeft: 32 }}>
            {experience.map((job, i) => (
              <div key={i} style={{ position: "relative" }}>
                {/* Dot */}
                <div style={{
                  position: "absolute", left: -36, top: 4,
                  width: 9, height: 9, borderRadius: "50%",
                  backgroundColor: "#4ade80",
                  boxShadow: "0 0 8px rgba(74,222,128,0.5)",
                }} />

                <div className="terminal-card" style={{ transition: "border-color 0.2s" }}>
                  {/* Header row */}
                  <div style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
                    <div>
                      <div style={{ color: "#4ade80", fontSize: 14, fontWeight: 700 }}>{job.title}</div>
                      <div style={{ color: "#e2e8f0", fontSize: 13, marginTop: 2 }}>{job.company}</div>
                    </div>
                    <div style={{ textAlign: "right" }}>
                      <div style={{ color: "#4a5568", fontSize: 12 }}>{job.period}</div>
                      <div style={{ color: "#4a5568", fontSize: 12 }}>{job.location}</div>
                      <span style={{ fontSize: 11, color: "#a78bfa", border: "1px solid rgba(167,139,250,0.3)", padding: "1px 8px", borderRadius: 4, marginTop: 4, display: "inline-block" }}>
                        {job.type}
                      </span>
                    </div>
                  </div>

                  {/* Bullets */}
                  <div style={{ borderTop: "1px solid #1a2540", paddingTop: 12 }}>
                    {job.bullets.map((b, j) => (
                      <div key={j} style={{ display: "flex", gap: 10, marginBottom: 6, fontSize: 13 }}>
                        <span style={{ color: "#4ade80", flexShrink: 0 }}>▸</span>
                        <span style={{ color: "#94a3b8", lineHeight: 1.6 }}>{b}</span>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}

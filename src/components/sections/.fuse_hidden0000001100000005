"use client";
import { funFacts } from "@/data/portfolio";

export function FunFacts() {
  return (
    <section id="funfacts" style={{ padding: "80px 24px", backgroundColor: "#0c1120" }}>
      <div style={{ maxWidth: 860, margin: "0 auto" }}>
        <div style={{ color: "#1a2540", fontSize: 13, marginBottom: 8 }}>// Beyond the code</div>
        <h2 style={{ color: "#4ade80", fontSize: 22, fontWeight: 700, marginBottom: 40 }}>
          $ cat hobbies.json
        </h2>

        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 24 }}>
          {/* Hobbies */}
          <div className="terminal-card">
            <div style={{ color: "#4ade80", fontSize: 13, marginBottom: 16 }}>hobbies: [</div>
            <div style={{ display: "flex", flexWrap: "wrap", gap: 10 }}>
              {funFacts.hobbies.map((h) => (
                <div key={h.label} style={{
                  border: "1px solid #1a2540",
                  borderRadius: 6,
                  padding: "8px 12px",
                  display: "flex",
                  alignItems: "center",
                  gap: 8,
                  fontSize: 13,
                  color: "#94a3b8",
                  transition: "border-color 0.2s",
                  cursor: "default",
                }}
                  onMouseEnter={e => (e.currentTarget as HTMLElement).style.borderColor = "#4ade80"}
                  onMouseLeave={e => (e.currentTarget as HTMLElement).style.borderColor = "#1a2540"}
                >
                  <span>{h.emoji}</span>
                  <span>{h.label}</span>
                </div>
              ))}
            </div>
            <div style={{ color: "#4ade80", fontSize: 13, marginTop: 16 }}>]</div>
          </div>

          {/* Fun facts */}
          <div className="terminal-card">
            <div style={{ color: "#4ade80", fontSize: 13, marginBottom: 16 }}>fun_facts: [</div>
            <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
              {funFacts.facts.map((f, i) => (
                <div key={i} style={{ display: "flex", gap: 10, fontSize: 13 }}>
                  <span>{f.emoji}</span>
                  <span style={{ color: "#94a3b8", lineHeight: 1.6 }}>{f.text}</span>
                </div>
              ))}
            </div>
            <div style={{ color: "#4ade80", fontSize: 13, marginTop: 16 }}>]</div>
          </div>
        </div>
      </div>
    </section>
  );
}
